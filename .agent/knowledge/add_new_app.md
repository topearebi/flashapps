# How to Add a New App

1. Run `mkdir apps/<name>`.
2. Initialize with `pnpm create vite . --template react`.
3. Update `package.json` to change the name.
4. Add shared deps: `@tope/ui` and `@tope/utils`.
5. Run `pnpm install`.
